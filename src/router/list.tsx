import { TorrentList } from "@/components/list/TorrentList";
import { FC } from "react";

export const List: FC = () => {
   return (
      <div className="">
         <TorrentList />
      </div>
   );
};
